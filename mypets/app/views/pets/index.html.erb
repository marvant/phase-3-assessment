<h1>Pet List</h1>
<ul>
	<% @pets.each do |pet| %>
	<li><%= pet.name %> |
	    <%= pet.breed %> | 
	    <%= pet.age %> | 
	    <%= pet.cute %> | 
	    <%= link_to 'Show', pet_path(pet)%></li>
	    <% end %>
</ul>